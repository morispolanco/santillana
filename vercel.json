{
  "version": 2,
  "builds": [
    {
      "src": "app.py",
      "use": "@vercel/python",
      "config": {
        "build": "pip install --no-deps -r requirements.txt && streamlit run app.py"
      }
    }
  ]
}
